> email의 골뱅이 앞부분만 가져오고 싶을 때.

``` sql
SUBSTRING_INDEX(email, '@', 1)
```

> 가장 마지막에 결재된 일시에서 30일 이전까지의 모든 결제 내역
``` sql
WHERE
	payment_date >= DATE_SUB((SELECT MAX(payment_date) FROM payment), INTERVAL 30 DAY)
```
